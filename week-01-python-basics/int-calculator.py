# This is a simple calculator that adds two numbers together.
# Take two numbers as input from the user and print the sum. Need to add int() function to convert the input strings to integers.
x = int(input("x="))
y = int(input("y="))

# Print the sum of the two numbers
print (x+y)