# This program takes a string input from the user, replaces all spaces with "...", and then prints the modified string.
input = input("Enter a string: ")

# Replace all spaces in the input string with "..." using the replace() method
input = input.replace(" ", "...")

print(input)