# This is a simple calculator that adds two floating point numbers.
# Take two numbers as input from the user and print the sum. Need to add float() function to convert the input strings to floating point numbers.
x = float(input("x = "))
y = float(input("y = "))

# Print the sum of the two numbers
print (x+y)